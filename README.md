# Aurora Insights
Aurora Insights is a high-performance business intelligence platform that leverages machine learning to provide actionable insights.
## Installation
1. Initialize the Go module: `go mod init aurora-insights-eng`
2. Install dependencies: `go get`
3. Run the application: `go run src/main.go`